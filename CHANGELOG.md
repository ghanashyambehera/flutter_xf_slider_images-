## 0.0.2

* Page dots under the carousel (one per image); size scales with `height`.
* Tap a dot to jump to that slide.
* Safer API: non-null `items`, `double height`, empty-list handling.
* Rounded image cards with loading and error placeholders.
* Optional `onPageChanged` and `onItemTap`.
* Configurable viewport, auto-play, infinite scroll, and indicator colors.

## 0.0.1

* Initial `FlutterXfSliderImages` widget wrapping `carousel_slider`.
* Network image slides with auto-play.
